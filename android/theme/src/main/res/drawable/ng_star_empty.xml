<?xml version="1.0" encoding="utf-8"?>
<!-- Unlit star, sized to match the lit star's glow footprint so the row stays aligned. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item
        android:width="22dp"
        android:height="22dp"
        android:left="6dp"
        android:top="6dp"
        android:right="6dp"
        android:bottom="6dp"
        android:drawable="@drawable/ng_star_core_empty" />
</layer-list>
