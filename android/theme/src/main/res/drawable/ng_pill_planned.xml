<?xml version="1.0" encoding="utf-8"?>
<!-- State pill — PLANNED (neutral / unlit). Quiet capsule for not-yet-started work
     (the source's `.phase-status-chip.planned`). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <solid android:color="@color/ng_surface_higher" />
    <stroke android:width="1dp" android:color="@color/ng_outline" />
    <corners android:radius="20dp" />
</shape>
